function isPrime(n)
{
    for(let i = 2; i < n; i++)
    {
        if(n % i == 0)return false;
    }
    return true;
}

function sumPrime(n)
{
    var s = 0;

    for(let i = 2; i < n; i++)
    {
        if(isPrime(i))s=s+i;
    }
    console.log('sumPrime is', s)
}

sumPrime(10)
